export enum MetricDistanceUnit {
  METER = 'METER',
  CENTIMETER = 'CENTIMETER',
  INCH = 'INCH',
  FEET = 'FEET',
  YARD = 'YARD',
}

export enum MetricTemperatureUnit {
  C = 'C',
  F = 'F',
  K = 'K',
}

export enum MetricType {
  DISTANCE = 'DISTANCE',
  TEMPERATURE = 'TEMPERATURE',
}
